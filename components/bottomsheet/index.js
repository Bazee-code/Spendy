import { View, Text } from 'react-native';
import React from 'react';

const BottomSheet = () => {
  return (
    <View>
      <Text style={{ color: '#000' }}>BottomSheet component</Text>
    </View>
  );
};

export default BottomSheet;
